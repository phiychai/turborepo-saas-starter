import type { Linter } from 'eslint';

/**
 * TypeScript declaration for @turborepo-saas-starter/eslint-config/base
 * Base ESLint configuration for all projects
 */
declare const baseConfig: Linter.Config[];
export default baseConfig;
