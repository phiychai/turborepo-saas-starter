/**
 * Bouncer configuration
 *
 * Bouncer v3 doesn't require explicit configuration - it's handled by the provider.
 * This file is kept for compatibility and future configuration needs.
 */
export default {};
